# Лабораторная работа №3: Простое веб-приложение (Node.js, Bootstrap)

**Дисциплина:** Программирование сетевых приложений

**Группа:** ИУ5Ц-64Б

**Студент:** Тошниёзов Жавохирбек

**Семестр:** 6

## Цель работы
Знакомство с инструментами разработки веб-приложений: Node.js, npm, Bootstrap. В ходе выполнения работы предстоит создать двухстраничное приложение с использованием компонентного подхода и реализовать индивидуальное задание по варианту.

## План работы
1. Установка Node.js и знакомство с npm
2. Инициализация проекта (npm init)
3. Подключение Bootstrap через npm
4. Реализация главной страницы с карточками товаров
5. Реализация страницы продукта
6. Навигация между страницами
7. Выполнение индивидуального задания по варианту

## Теоретическая часть

### Node.js
Node.js — программная платформа, основанная на движке V8 (Google Chrome), позволяющая выполнять JavaScript код вне браузера — на сервере или локальном компьютере. В отличие от браузерного JS, Node.js предоставляет доступ к файловой системе, сети и другим возможностям операционной системы.

### npm (Node Package Manager)
npm — стандартный пакетный менеджер для Node.js. С его помощью можно устанавливать сторонние библиотеки и управлять зависимостями проекта. Все установленные пакеты сохраняются в папку `node_modules`, а информация о них — в файлах `package.json` и `package-lock.json`.

### package.json
Файл, содержащий метаинформацию о проекте: название, версию, описание, список зависимостей, скрипты для запуска. Является основным конфигурационным файлом Node.js-проекта.

### Bootstrap
Популярный CSS-фреймворк, предоставляющий готовые компоненты для быстрой верстки адаптивных интерфейсов. В работе использовался Bootstrap 5, установленный через npm.

### Компонентный подход
Архитектурный паттерн, при котором интерфейс разбивается на независимые, переиспользуемые блоки — компоненты. Каждый компонент отвечает за свою часть интерфейса и может быть переиспользован на разных страницах.

## Ход выполнения

### 1. Установка Node.js и инициализация проекта
- Установлен Node.js (рекомендуется использовать nvm для Windows или Homebrew для macOS)
- Создана папка проекта
- Выполнена команда `npm init` для создания `package.json`

### 2. Подключение Bootstrap
Выполнена установка Bootstrap через npm:
```bash
npm i bootstrap
```

### 3. Создание корневого элемента
В `index.html` добавлен div с id="root" — точка входа для всех страниц:
```html
<div id="root"></div>
<script src="main.js" type="module"></script>
```

### 4. Реализация компонентов
Компонент ButtonComponent (`components/button/index.js`):
* Отвечает за отрисовку кнопки
* Принимает данные и обработчик клика

Компонент ProductCardComponent (`components/product-card/index.js`):
* Отрисовывает карточку товара с изображением, заголовком, описанием
* Содержит кнопку для перехода на страницу деталей
* Поддерживает передачу обработчика события

Компонент BackButtonComponent (`components/back-button/index.js`):
* Кнопка возврата на главную страницу

Компонент ProductComponent (`components/product/index.js`):
* Отрисовывает детальную информацию о товаре в горизонтальной карточке

### 5. Реализация страниц
Главная страница (`pages/home/index.js`):
* Создает контейнер для карточек (grid)
* Получает массив данных о товарах
* Для каждого товара создает компонент ProductCardComponent
* При клике на карточку переходит на страницу продукта

Страница продукта (`pages/product/index.js`):
* Принимает id выбранного товара
* Отображает детальную информацию о товаре (описание слева, изображение справа)
* Содержит кнопку возврата на главную страницу

Страница "Обо мне" (`pages/about/index.js`):
* Отображает информацию о студенте, навыках и контактах

Страница калькулятора (`pages/calculator/index.js`):
* Реализует полнофункциональный калькулятор с поддержкой клавиатуры

### 6. Роутинг и навигация
В файле `main.js` реализована маршрутизация на основе хеша:
```js
import {HomePage} from "./pages/home/index.js";
import {AboutPage} from "./pages/about/index.js";
import {CalculatorPage} from "./pages/calculator/index.js";

const root = document.getElementById('root');

function router() {
    const hash = window.location.hash || '#home';
    // отрисовка соответствующей страницы
}

window.addEventListener('hashchange', router);
window.addEventListener('load', router);
```
Переход между страницами осуществляется через изменение хеша в URL, что позволяет реализовать SPA-подход без перезагрузки страницы.

### 7. Выполнение индивидуального задания
**Вариант:** фотографии + группа кнопок

1. **Тема "Фотографии"**:
   - На главной странице отображаются 3 карточки с аниме-тайтлами (Магическая битва, Клинок, рассекающий демонов, Гачи)
   - Каждая карточка содержит изображение, название и краткое описание
   - Данные загружаются из метода `getData()` главной страницы

2. **Компонент "Группа кнопок"**:
   - На странице детального просмотра реализован компонент `BackButtonComponent` для возврата на главную страницу
   - Карточки на главной странице содержат кнопки для перехода к деталям
   - Все кнопки выполнены в едином стиле с использованием оранжевой цветовой схемы

3. **Результат**: Получено многстраничное приложение с компонентной архитектурой и навигацией между страницами.

## Структура проекта
```
├── node_modules/
├── .gitignore
├── package.json
├── package-lock.json
├── index.html              # Главная страница (SPA-оболочка)
├── main.js                 # Главный JS файл (роутер SPA)
├── style.css               # Все стили проекта
├── DJavakhirDJon.png       # Логотип в навигации
├── djava.png               # Аватар на странице "Обо мне"
├── manga.png               # Изображение
├── readme.md               # Отчёт
├── pages/
│   ├── home/
│   │   └── index.js        # Класс HomePage (3 карточки)
│   ├── about/
│   │   └── index.js        # Класс AboutPage
│   ├── calculator/
│   │   └── index.js        # Класс CalculatorPage
│   └── product/
│       └── index.js        # Класс ProductPage
└── components/
    ├── product-card/
    │   └── index.js        # Компонент карточки товара
    ├── product/
    │   └── index.js        # Компонент детального просмотра
    ├── back-button/
    │   └── index.js        # Компонент кнопки назад
    └── button/
        └── index.js        # Компонент кнопки
```

## Вывод
В ходе выполнения лабораторной работы я освоил инструменты современной веб-разработки: Node.js, npm, Bootstrap. Создано многстраничное приложение с компонентной архитектурой, реализована навигация между страницами без перезагрузки (SPA-подход) с использованием хеш-роутинга.

Выполнено индивидуальное задание: тема "Фотографии" с тремя карточками аниме-тайтлов, каждая из которых имеет страницу детального просмотра с описанием слева и изображением справа. Реализованы дополнительные страницы: калькулятор и "Обо мне".

Полученные навыки позволяют структурировать фронтенд-проекты, использовать пакетный менеджер для управления зависимостями, создавать переиспользуемые компоненты и реализовывать клиентскую маршрутизацию. Такой подход является основой для разработки современных одностраничных приложений (SPA).
